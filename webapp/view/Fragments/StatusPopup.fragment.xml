<core:FragmentDefinition xmlns="sap.m" xmlns:core="sap.ui.core">
	<Table items="{path: 'StatusPopupModel>/popupData', templateShareable: true}" noDataText="{i18n>lblnoData}" mode="None">
		<columns>
			<Column>
				<Text text="{i18n>lblCharNo}"/>
			</Column>
			<Column>
				<Text text="{i18n>lblStatus}"/>
			</Column>
			<Column minScreenWidth="Tablet" demandPopin="false">
				<Text text="{i18n>lblCharNo}"/>
			</Column>
		</columns>
		<items>
			<ColumnListItem>
				<cells>
					<Text text="{StatusPopupModel>CharNo}"/>
					<Text text="{StatusPopupModel>Status}" />
					<Text text="{StatusPopupModel>CharNo}"/>
				</cells>
			</ColumnListItem>
		</items>
	</Table>
</core:FragmentDefinition>